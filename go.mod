module github.com/itsmangooo/weedout-cli

// 1.22 rather than the newest release: this binary gets built by whatever Go
// a contributor happens to have, and nothing here needs a recent language
// feature. Raising it buys nothing and excludes people.
go 1.22
